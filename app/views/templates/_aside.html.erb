<aside id="template_aside">
  <nav>
    <div>
      <ul>
        <li></li>
      </ul>
    </div>
    <div>
      <h3>카테고리</h3>
      <ul>
        <% @template_categories.each do |template_category| %>
        <li <% if template_category.id==params[:template_category_id].to_i %>class="active"<% end %>><%=link_to template_category.name,templates_path(:template_category_id=>template_category.id) %></li>
        <% end %>
      </ul>
    </div>
  </nav>
</aside>
