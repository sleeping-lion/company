<section class="template_recommend_slider_by_categories">
  <div class="template_recommend_category_list">
    <ul class="nav nav-pills">>
      <% unless @template_categories.empty? %>
      <% @template_categories.each do |template_category| %>
      <li><%=link_to template_category.name,templates_path(:template_category_id=>template_category) %></li>
      <% end %>
      <% else %>
      <% end %>
    </ul>
  </div>
  <div class="scrollable">
    <% unless @template_sliders.empty? %>
    <ul class="items">
      <% @template_sliders.each_slice(6) do |template5| %>	
      <li>
        <% template5.each_with_index do |template,index| %>
	<article class="template<% if(index==5) %> nm<% end %>">
		<div class="template_img">
			<input type="hidden" value="<%=template.main_screenshot %>" />
			<%=link_to template_path(:id=>template,:product=>template.template_type.product_id),:html=>{:target=>'_blank',:class=>'detail_link'} do %>
				<%=image_tag template.small_screenshot %>
			<% end %>
		</div>
		<div class="bottom_link">
			<%=link_to '데모',:html=>{:target=>'_blank',:class=>'demo_link'} %><span class="devide_word">|</span>
			<%=link_to '상세',template_path(template,:product=>template.template_type.product_id,),:html=>{:target=>'_blank',:class=>'detail_link'}  %>
		</div>
	</article>
			<% end %>
			</li>
		<% end %>
		</ul>
		<% else %>
		<p></p>
		<% end %>
	</div>
</section>